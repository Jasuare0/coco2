<% if(carousel.length != null && carousel.length != 0){ %>

<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
    <ol class="carousel-indicators">

        <% for(let m=0; m<carousel.length; m++){%>

            <% if(m==0){ %>
              <li data-target="#carouselExampleIndicators" data-slide-to="<%= m %>" class="active"></li>

            <% }else{ %>
              <li data-target="#carouselExampleIndicators" data-slide-to="<%= m %>"></li>
            
            <% } %>

        <% } %>

    </ol>
    <div class="carousel-inner">

      <% if(carousel.length != null){ %>
        <% for(let z=0; z<carousel.length; z++){%>

            <% if(z==0){ %>
              <div class="carousel-item active">
                <img src="../images/carousel/<%= carousel[z].imagen %>" class="d-block w-100" alt="...">
                </div>
                
            <% }else{ %>
              <div class="carousel-item">
                <img src="../images/carousel/<%= carousel[z].imagen %>" class="d-block w-100" alt="...">
                </div>
      
            <% } %>

        <% } %>

      <% } %>

    </div>
    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
</div>

<% } %>
